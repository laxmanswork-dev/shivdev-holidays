# /images/brand

`shivdev-holidays-logo.png` is the real site logo, used by the header,
SEO/Open Graph metadata, and structured data — see `src/data/site.js`,
`src/components/layout/Header.jsx`.

Everything else in this folder isn't referenced anywhere in the code —
leftover uploads from an earlier attempt at full-bleed section
background photos on the homepage and the Destinations page (since
removed). Safe to delete; nothing will break either way since the app
never reads them.
