/** Keyboard-only "skip to content" link, required for accessible navigation. */
export function SkipLink() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to content
    </a>
  );
}
